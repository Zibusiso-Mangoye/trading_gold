version: '3'

services:
  streamlit_dashboard:
    image: dashboard:latest
    container_name: streamlit_dashboard
    ports:
      - "8501:8501"
    networks:
      - airflow_default
    volumes:
      - ./:/usr/dashboard

networks:
  airflow_default:
    external: true